import PageLoader from '@/components/PageLoader';
import React from 'react'

const Loading = () => {
  return (
    <PageLoader text="Hang tight! We're fetching your projects." />
  );
};

export default Loading;
